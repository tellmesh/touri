version: '1.0'
project:
  name: touri
  type: [python]
  description: 'touri — TellMesh URI package'
versioning:
  strategy: semver
  files: [pyproject.toml:version]
git:
  commit:
    strategy: conventional
    scope: touri
  tag:
    enabled: true
    prefix: v
strategies:
  python:
    test: pytest tests/ -q
    build: python -m build
    publish: twine upload dist/touri-{version}*
    publish_enabled: true
registries:
  pypi:
    url: https://pypi.org/simple/
    token_env: PYPI_TOKEN
